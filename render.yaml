services:
  - type: web
    name: vitaltime
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: VITALTIME_TIME_SCALE
        value: "1.0"
